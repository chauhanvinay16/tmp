import 'package:flutter/material.dart';

class ScreenRepo extends StatefulWidget {
  const ScreenRepo({super.key});

  @override
  State<ScreenRepo> createState() => _ScreenRepoState();
}

class _ScreenRepoState extends State<ScreenRepo> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fack Reposetry'),
      ),
      body: Column(

      ),
    );
  }
}
